{
    "name": "jamatic-generator",
    "version": "0.0.1",
    "description": "A program to convert DB schema to deployable primsa application",
    "main": "dist/src/app.js",
    "scripts": {
        "prisma:generate": "prisma generate",
        "prisma:introspect": "prisma introspect",
        "start": "tsc && node dist/src/app.js",
        "clean": "rm -rf dist",
        "build": "npm -s run clean && tsc"
    },
    "author": "zootopialover",
    "license": "MIT",
    "dependencies": {
        "node-stringbuilder": "^2.2.4",
        "prisma-generate-schema": "^1.34.12",
        "ts-morph": "^10.0.2"
    },
    "devDependencies": {
        "@types/node": "^14.14.39",
        "tslint": "^5.12.1",
        "typescript": "^3.9.7"
    }
}
